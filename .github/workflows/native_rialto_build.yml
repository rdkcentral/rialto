name: Native Rialto Build

on:
  pull_request:
    branches: [ "master" ]
  push:
    branches: [ "Aadit_NativeRialtoBuild" ]
  workflow_dispatch:

jobs:
  build: 
      runs-on: ubuntu-20.04 

      steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install build-essential
          sudo apt-get install cmake

      - name: Build Rialto
        run:
          cmake build -DRIALTO_LOG_FATAL_ENABLED=OFF -DRIALTO_LOG_ERROR_ENABLED=OFF -DRIALTO_LOG_WARN_ENABLED=OFF -DRIALTO_LOG_MIL_ENABLED=OFF -DRIALTO_LOG_INFO_ENABLED=OFF -DRIALTO_LOG_DEBUG_ENABLED=OFF
          cmake --build build

      - name: Report Build Status
        if: ${{ always() }}
        run: |
          if [[ ${{ job.status }} == 'true' ]];
          then
            echo "Build Succeeded!"
            exit 0
          else
            echo "Build Failed!"
            exit 1
          fi

 
        